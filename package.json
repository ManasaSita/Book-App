{
    "name": "bookshelf-app",
    "version": "1.0.0",
    "scripts": {
        "install-all": "npm install && cd server && npm install && cd ../client && npm install"
    },
    "devDependencies": {},
    "dependencies": {
        "axios": "^1.7.2",
        "react-icons": "^5.3.0",
        "react-router-dom": "^6.25.0"
    }
}
